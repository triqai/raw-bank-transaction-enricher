# Triqai API Configuration
# Get your free API key at https://triqai.com/dashboard/api-keys

# Required: Your Triqai API key
TRIQAI_API_KEY=your_api_key_here

# ── Rate limit settings ────────────────────────────────────────────────────────
# Free plan limits: 1 RPS and 2 concurrent in-flight requests.
# Raise these only if you are on a paid plan that allows it.

# Maximum parallel requests in-flight at once (free plan: 2)
# MAX_CONCURRENT_REQUESTS=2

# Minimum delay between dispatching requests in seconds (free plan: 1.0 = 1 RPS)
# REQUEST_DELAY=1.0
