# Triqai API Configuration
# Get your free API key at https://www.triqai.com/dashboard/api-keys

# Required: Your Triqai API key
TRIQAI_API_KEY=your_api_key_here

# Optional: Maximum number of parallel API requests (default: 5)
# Increase for large datasets, decrease if hitting rate limits
# MAX_CONCURRENT_REQUESTS=5

# Optional: Minimum delay between requests in seconds (default: 0.1)
# REQUEST_DELAY=0.1
